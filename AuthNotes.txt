https://docs.amplify.aws/start/getting-started/setup/q/integration/vue/
<amplify-authenticator>:
https://docs.amplify.aws/start/getting-started/auth/q/integration/vue/  

UI Components: Authenticator component:
https://docs.amplify.aws/ui/auth/authenticator/q/framework/vue/

Amplify UI:
https://ui.docs.amplify.aws/?platform=vue


- Setup:
npm install -g @aws-amplify/cli
amplify configure

- Initialize a new backend:
amplify init

- Create the Authentication Service:
amplify add auth
(Choose if federated or not, etc)

To deploy the service, run the push command.
"amplify push" will build all your local backend resources and provision it in the cloud
"amplify publish" will build all your local backend and 
frontend resources (if you have hosting category added) 
and provision it in the cloud

//run amplify pull to sync your aws-exports.js with your cloud backend:
amplify pull
-------------------------------------------------------------------------------------------
The users are managed under a Cognito User Pool 
(coolaquas8c3ed8a4_userpool_8c3ed8a4-dev)
-------------------------------------------------------------------------------------------
Frontend setup:
1. Install Amplify Libraries - We need to install the Amplify dependencies in our Vue application. 
You can install them with this command:

//amplify sdk:
$npm install aws-amplify
//amplify-vue ui components:
npm install @aws-amplify/ui-components
//Vue Amplify UI Components:
npm install @aws-amplify/ui-vue

2. Configure Our Application
We need to add Amplify to our Vue application. Open up the main.js file, and add the following after the last import line:
~~~ js
import Amplify from 'aws-amplify';
import aws_exports from './aws-exports';
import {
  applyPolyfills,
  defineCustomElements,
} from '@aws-amplify/ui-components/loader';

Amplify.configure(aws_exports);
applyPolyfills().then(() => {
  defineCustomElements(window);
});

~~~





